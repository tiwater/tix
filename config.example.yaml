# TiClaw 配置示例
# 复制为 ~/.ticlaw/config.yaml 并填入实际值

channels:
  # Config-driven: only channels with a block and enabled !== false are started.
  # acp:
  #   enabled: true
  #   hub_url: "https://acp-relay.example.com"
  #
  # discord:
  #   enabled: true
  #   token: "your-discord-bot-token"

  feishu:
    enabled: true
    app_id: "cli_xxxxxxxx"       # 飞书应用 App ID
    app_secret: "xxxxxxxx"       # 飞书应用 App Secret

# assistant_name: Andy          # 机器人名称，触发格式 @Andy

# Proxy (required for China; LLM calls, Discord, Feishu all use it)
# proxy: "http://127.0.0.1:7890"

# llm:
#   api_key: "your-bigmodel-key"        # BigModel API key
#   model: "google/gemini-2.5-flash"

# Generic control-plane enrollment (TOFU + out-of-band verification)
# control_plane:
#   url: "https://control-plane.example.com"
#   enrollment_mode: "tofu_oob"
#   runtime_id: "runtime-prod-01"

# OpenClaw-compatible skills registry
# skills:
#   directories:
#     - "~/.ticlaw/skills"
#     - "./skills"
#   admin_only: true
#   allow_level3: false
#   auto_enable: false
